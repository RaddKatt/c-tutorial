/*
A prototype for a function tells the compiler:
	1. What the function will return (ex: int)
	2. What the function will be called (ex: my_func)
	3. What arguments the function will be passed (ex: int x, int y)
		Note: A function may accept 0 or more arguments


	Format:		return-type	function_name	( arg_type arg1, ..., arg_type argN ); 
	Example:	int 		my_func		(int x, int y);
*/
